import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center"
  },
  headerText: {
    fontSize: 29,
    textAlign: "center",
    flexWrap: "wrap",
    fontWeight: "600"
  },
});
